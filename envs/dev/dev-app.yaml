apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/<your-user>/helm-gitops-demo.git
    targetRevision: dev
    path: charts/myapp
    helm:
      valueFiles:
        - ../../envs/dev/values-dev.yaml    #relative to source.path
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
